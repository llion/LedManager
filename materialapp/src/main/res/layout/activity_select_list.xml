<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".SelectListActivity" >

    <android.support.v7.widget.Toolbar
        android:id="@+id/activity_select_list_toolbar"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:background="?attr/colorPrimary"
        android:theme="@style/ThemeOverlay.AppCompat.Dark.ActionBar"
        app:popupTheme="@style/ThemeOverlay.AppCompat.Light"
        android:elevation="4dp"

    />
    <!--<TextView-->
        <!--android:layout_width="match_parent"-->
        <!--android:layout_height="@dimen/dp_2"-->
        <!--android:background="@color/gray"/>-->

    <com.clt.ledmanager.ui.PagerSlidingTabStrip
        android:layout_below="@id/activity_select_list_toolbar"
        android:id="@+id/main_tabs"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        app:pstsShouldExpand="true"
        app:pstsUnderlineHeight="4dp"
        app:pstsIndicatorHeight="3dp"

        app:selectedTabTextSize="15dp"
        app:selectedTabTextColor="@android:color/white"

        app:tabTextSize="13dp"
        app:tabTextColor="@android:color/holo_blue_dark"

        app:pstsIndicatorColor="@android:color/holo_blue_dark"
        app:pstsDividerColor="@android:color/transparent"
        app:pstsTabBackground="@drawable/background_tab"
        android:background="#FFFFFF" />


    <android.support.v4.view.ViewPager
        android:id="@+id/main_viewPager"
        android:layout_below="@id/main_tabs"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" >
    </android.support.v4.view.ViewPager>

</RelativeLayout>